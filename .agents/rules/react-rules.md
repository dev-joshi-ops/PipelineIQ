# Deprecated React Rules

This file is intentionally retained only as a tombstone for the repository's earlier scaffold.

- Do **not** use this file for new work.
- Do **not** scaffold React components, Redux slices, frontend routes, or browser-only workflows from this repository unless an ADR explicitly reintroduces them.
- Use the MCP-first personas and skills instead.

# Deprecated FastAPI Rules

This file is intentionally retained only as a tombstone for the repository's earlier scaffold.

- Do **not** use this file for new work.
- Do **not** scaffold FastAPI routes, REST endpoints, SQLAlchemy models, Alembic migrations, or web-service middleware from this repository unless an ADR explicitly reintroduces them.
- Use `.agents/rules/mcp-builder.md` and `.agents/skills/scaffold-mcp-server/SKILL.md` instead.

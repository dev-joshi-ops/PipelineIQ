---
name: scaffold-backend-endpoint
description: Deprecated placeholder. This repository no longer uses the FastAPI backend endpoint scaffold. Use `scaffold-mcp-server` instead when building new capabilities.
---

# Deprecated Backend Scaffold

This skill is no longer active for this repository.

- Do **not** use it to generate new code.
- Use `.agents/skills/scaffold-mcp-server/SKILL.md` instead.
